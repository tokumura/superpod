<%= form_for(@host_config) do |f| %>
  <% if @host_config.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@host_config.errors.count, "error") %> prohibited this host_config from being saved:</h2>

      <ul>
      <% @host_config.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :host %><br>
    <%= f.text_field :host %>
  </div>
  <div class="field">
    <%= f.label :port %><br>
    <%= f.text_field :port %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
